@*Source: http://www.growingwiththeweb.com/2012/12/aspnet-mvc-display-and-editor-templates.html*@

@model int?

@using System.Web.Mvc;

@{
    var items = (List<SelectListItem>)ViewBag.RadioButtonList;
}

<ul class="radio-list">
    @foreach (var item in items)
    {
        <li>
            @{
        var radioId =
            ViewData.TemplateInfo.GetFullHtmlFieldId(item.Value);
        var checkedClass = (item.Value == Model.ToString()
                            ? "checked"
                            : string.Empty);
                            <input type="radio"
                                   id="@radioId"
                                   name="@ViewData.TemplateInfo.HtmlFieldPrefix"
                                   value="@item.Value"
                                   checked="@checkedClass" />
                            <label for="@radioId">@item.Text</label>
            }
        </li>
    }
</ul>



