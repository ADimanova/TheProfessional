﻿@model DateTime?

@{
    DateTime dt = DateTime.Now;

    if (Model != null)
    {

        dt = (System.DateTime)Model;
    }

    @Html.TextBox(String.Empty, null, new { @class = "form-control datecontrol bordered", id="datepicker", type = "text", })
}

