@using Professional.Web.Models.Shared

@model ICollection<NavigationItem>

@if (@Model.Count > 0)
{
    foreach (var item in Model)
    {
        <a href="#" class="list-group-item">@item.Content</a>
    }
}
else
{
    <a href="#" class="list-group-item">No items here.</a>
}


